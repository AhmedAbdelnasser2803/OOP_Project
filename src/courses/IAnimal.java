package courses;

public interface IAnimal {
    /*
     * interface : is the type of the class have the abstract method
     * have only header of the function
     * l can implement function or method through the class
     * we should implement the all method in interface  */
//    set only the header of the function
    public void eat ();
    public void travel();
}
